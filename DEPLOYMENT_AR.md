# موقع شركة بن كليلة العامري

هذه نسخة مستقلة من الموقع مع بطاقات المنتجات ثلاثية الأبعاد، التحكم في الألوان والشفافية والضبابية وأبعاد الصور، والواجهة العربية والإنجليزية.

## التشغيل محليًا

يتطلب المشروع Node.js 20 أو أحدث.

```bash
npm install
ADMIN_PASSWORD=SALALAH2026 npm run dev
```

بعد التشغيل افتح `http://localhost:3000`.

## الحفظ والنشر

تقرأ واجهة العملاء آخر نسخة منشورة من المسار `/api/site-state` بدل الاعتماد على IndexedDB وحده. عند حفظ منتج أو صورة أو إعدادات من لوحة التحكم، ينفذ الموقع حفظًا محليًا ثم يرسل نسخة كاملة إلى الخادم تلقائيًا. يوجد مسار حفظ يدوي داخل لوحة الإدارة حسب الواجهة الحالية، ويعمل الحفظ التلقائي عند تنفيذ أي تغيير محفوظ.

تُخزّن النسخة المركزية في `data/site-state.json`. يجب أن تكون مجلدات `data` قابلة للكتابة على الخادم وأن تكون الاستضافة دائمة؛ إذا كانت الاستضافة مؤقتة أو تعيد إنشاء القرص عند كل نشر، فيلزم ربط هذا المسار بقاعدة بيانات أو تخزين دائم قبل الاستخدام التجاري.

## كلمة مرور الإدارة

كلمة المرور الافتراضية المطلوبة هي `SALALAH2026`. يفضّل تغييرها في إعدادات البيئة وعدم وضع كلمة المرور الحقيقية داخل مستودع عام:

```bash
ADMIN_PASSWORD='ضع-كلمة-مرور-جديدة-هنا' npm start
```

## رفع الصور

تحتوي لوحة الإدارة على اختيار ملف `image/*`، ولذلك يمكن رفع صورة مباشرة من استديو الهاتف عند فتح الموقع من الهاتف. بعد اختيار الصورة وحفظ المنتج، تُحفظ الصورة ضمن بيانات النسخة المنشورة. يفضّل استخدام صور مضغوطة عندما تكون كثيرة أو كبيرة حتى لا يتجاوز حجم طلب الحفظ.

## رابط العملاء وQR

شارك رابط النسخة المنشورة نفسها مع العملاء أو حوّله إلى QR. يجب عدم مشاركة رابط لوحة الإدارة منفصلًا؛ لوحة الإدارة تفتح من داخل الموقع وتطلب كلمة المرور.

## ملاحظة مهمة

ملف HTML الثابت وحده لا يمكنه مشاركة تعديلات لوحة الإدارة بين الأجهزة. لهذا تتضمن هذه النسخة خادم Express ومسار الحالة المركزية. عند وضعها على استضافة دائمة، سيرى كل عميل آخر نسخة أرسلتها لوحة الإدارة بدل النسخة الافتراضية.

## الاختبار

```bash
npm run lint
npm run build
npm start
```

تم اختبار `lint` و`build` بنجاح في بيئة التطوير.

## آخر التعديلات

تتضمن النسخة بطاقات مستطيلة ثلاثية الأبعاد، ألوان كحلي وأزرق فاتح وذهبي وشفاف، تحكمًا مستقلًا لكل منتج في عرض الصورة وارتفاعها وملاءمتها وشفافيتها وضبابيتها، وتحكمًا في شفافية البطاقة وضبابيتها، إضافة إلى الترجمة العربية والإنجليزية في تفاصيل المنتج.

© شركة بن كليلة العامري
